spring:
  application:
    name: tpf-service-automation-sgb
  cloud:
    config:
      #uri: http://localhost:8888
      uri: http://10.1.69.14:8888
      fail-fast: true
      retry.initial-interval: 5000
      retry.max-attempts: 100
      retry.max-interval: 10000
      retry.multiplier: 2
---
spring:
  profiles: dev
  cloud:
    config:
      uri: http://tpf-service-config:8888
      fail-fast: true
      retry.initial-interval: 5000
      retry.max-attempts: 100
      retry.max-interval: 10000
      retry.multiplier: 2
---
spring:
  profiles: uat
  cloud:
    config:
      uri: http://tpf-service-config:8888
      fail-fast: true
      retry.initial-interval: 5000
      retry.max-attempts: 100
      retry.max-interval: 10000
      retry.multiplier: 2
---
spring:
  profiles: pro
  cloud:
    config:
      uri: http://tpf-service-config:8888
      fail-fast: true
      retry.initial-interval: 5000
      retry.max-attempts: 100
      retry.max-interval: 10000
      retry.multiplier: 2