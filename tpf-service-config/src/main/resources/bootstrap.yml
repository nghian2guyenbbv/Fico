server:
  port: 8888
spring:
  application:
    name: tpf-service-config
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          searchLocations: file:/config-repo
---
spring:
  profiles: dev
  cloud:
    config:
      server:
        native:
          searchLocations: file:/config-repo
---
spring:
  profiles: uat
  cloud:
    config:
      server:
        native:
          searchLocations: file:/config-repo
---
spring:
  profiles: pro
  cloud:
    config:
      server:
        native:
          searchLocations: file:/config-repo