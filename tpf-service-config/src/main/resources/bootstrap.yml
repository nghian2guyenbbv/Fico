server:
  port: 8888
spring:
  application:
    name: tpf-service-config
  rabbitmq:
    host: localhost
    port: 5672
    username: tpf-service-config
    password: tpf-service-config
    app-id: tpf-service-config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/daongoc315/tpf-config.git
---
spring:
  profiles: dev
  rabbitmq:
    host: tpf-opensource-rabbitmq
  cloud:
    config:
      server:
        git:
          uri: http://tpf-opensource-git/service-app/tpf-config.git
          username: service-app
          password: service-app
---
spring:
  profiles: uat
  rabbitmq:
    host: tpf-opensource-rabbitmq
  cloud:
    config:
      server:
        git:
          uri: http://tpf-opensource-git/service-app/tpf-config.git
          username: service-app
          password: service-app
---
spring:
  profiles: pro
  rabbitmq:
    host: tpf-opensource-rabbitmq
  cloud:
    config:
      server:
        git:
          uri: http://tpf-opensource-git/service-app/tpf-config.git
          username: service-app
          password: service-app
